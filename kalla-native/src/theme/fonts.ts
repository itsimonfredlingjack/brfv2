import {
  InstrumentSans_400Regular,
  InstrumentSans_500Medium,
  InstrumentSans_600SemiBold,
  InstrumentSans_700Bold,
} from '@expo-google-fonts/instrument-sans'
import {
  InstrumentSerif_400Regular,
  InstrumentSerif_400Regular_Italic,
} from '@expo-google-fonts/instrument-serif'
import { JetBrainsMono_500Medium, JetBrainsMono_700Bold } from '@expo-google-fonts/jetbrains-mono'

/** Passed to `useFonts()` in the root layout. Instrument Sans carries UI
 * chrome, Instrument Serif carries the answer and every quote (it is what
 * makes belagt prose read as *written*, not chatbot output), and JetBrains
 * Mono carries every label, meta line and score — the "data" register. */
export const fontAssets = {
  InstrumentSans_400Regular,
  InstrumentSans_500Medium,
  InstrumentSans_600SemiBold,
  InstrumentSans_700Bold,
  InstrumentSerif_400Regular,
  InstrumentSerif_400Regular_Italic,
  JetBrainsMono_500Medium,
  JetBrainsMono_700Bold,
}
