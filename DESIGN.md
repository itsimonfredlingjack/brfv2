# DESIGN.md — Design Context

> Evidence-based snapshot of this project's visual design.
> Observations describe the starting point, not rules. See "Intent & Direction".
> Generated by /design-context on 2026-08-07. **Updated 2026-08-08** after the
> unified-system pass landed on desktop web (`docs/design/2026-08-08-unified-system/`,
> plan in `IMPLEMENTATION.md` there): sans-first typography, one button primitive,
> four locked breakpoints, one focus ring, unified empty states, keyboard flow.
> Sections below describe the current state; struck-through legacy is gone, not deprecated.

## Evidence Sources

- Code/config: `brfv2-mockup/src/theme.css` (canonical tokens + primitive UI), `brfv2-mockup/src/App.css` (shell, chat, docs, dialogs), domain CSS under `brfv2-mockup/src/components/*.css`, `brfv2-mockup/src/components/TraffMark.jsx`, `brfv2-mockup/src/components/website/site.css` (published site, separate system), `xs_mobilapp/src/styles/tokens.css`, `kalla-native/src/theme/tokens.ts`, `conductor/product-guidelines.md` (voice / a11y targets, not visual tokens)
- Rendered UI: unified-system mockups and screenshots in `docs/design/2026-08-08-unified-system/shots/`; live-harness evidence in `docs/evidence/inkommande-readable-layout.png`, `inkommande-empty-state.png`, `inkommande-keyboard-flow.png`
- External reference: none (identity source `Traff-identitet-v2.dc.html` cited in code, not in repo)
- Not available: Figma, Storybook, identity HTML source, live screenshots / computed styles, dark mode on desktop web

## Intent & Direction

- Current direction: **decided 2026-08-08** — the desktop web UI runs the unified system from `docs/design/2026-08-08-unified-system/` (approved section mockups + `IMPLEMENTATION.md` beslut 1–3).
- Scope/notes: Identitet v2's colour semantics, paper ground and epistemic voice are kept; its serif display layer is deliberately replaced by a humanist sans (Poppins) with Inter as the UI face — a digital-first readability decision, not a drift. Phone clients still carry the serif answer layer (see Open Questions). The design decisions are locked in code by `brfv2-mockup/src/design-lock.test.js`.

## Aesthetic Character (interpretation)

Quiet, paper-forward operational UI for volunteer boards: warm off-white workspace, raised white panels, warm near-black ink. Typography does epistemic work with weight, not serifs — display sans asserts (answers, titles, quotes), sans explains (chrome, labels), mono measures (states, dates, page numbers). Colour is reserved for meaning (grounded / refusal / searching / machine failure), not decoration. Density is tool-like without dashboard chrome; the document PDF is the one pure-white surface.

## Color

Desktop web (`brfv2-mockup/src/theme.css`) — `color-scheme: light` only. Dark mode: **none** on desktop web; **full** on mobile PWA via `prefers-color-scheme`; native is dark-chrome by default.

| Role | Value | Source | Notes |
|---|---|---|---|
| Ground (papper) | `#F7F5F3` | `--papper` / `--background` | Warm, never pure white |
| Surface steps | `#FBFAF9` / `#F7F5F3` / `#EFECE8` | `--surface-1..3` | Quiet section / inset / pressed |
| Raised field / card | `#FFFFFF` | `--field` / `--card` | Inputs and cards lift off paper |
| Sidebar | `#FFFFFF` | `--sidebar` | Raised vs workspace paper |
| Ink | `#37322F` | `--ink` / `--foreground` | Never `#000` |
| Ink muted / subtle | `#605A57` / `#6F6A66` | `--ink-muted` / `--ink-subtle` | Secondary / meta |
| Border / strong / input | `#E4E0DC` / `#CDC7C1` / `#DAD5D0` | `--border*` / `--input` | Warm greys |
| Primary action (fill) | ink `#37322F` → hover `#262220` | `--primary` | **Not** blue; identity locks Handling to links |
| Handling (links/text actions) | `#1A4FD0` / hover `#123CA8` | `--handling` | Soft wash `#E9EEFB` |
| Sökljus (searching) | `#3A6FD8` (ring) / `#2C5CBE` (text) | `--sokljus*` | Work-in-progress only |
| Belagt (verified) | `#137855` + soft/border | `--belagt` / `--ok*` | Not generic “success” |
| Vägran (no proof) | `#8A5D06` + soft/border | `--vagran` / `--warn*` | Refusal is correct behaviour |
| Avbrott (machinery) | `#A82F22` + soft/border | `--avbrott` / `--danger*` | Failures / destructive ops only |
| Remsgul (highlight) | `#FFD600` | `--remsgul` | Only inside document paper |
| Brand (on paper) | ink monochrome | `--brand` | Mark never takes state colour in brand variant |

**Mobile PWA light** shares the same hex meanings under different names (`--paper`, `--grounded`, `--refusal`, `--action`). **Mobile dark** and **native** use identity shell values (`#07080B` skal, mid-tone state colours, action blues that differ slightly: PWA dark `#7EA2FF`, native `#2E6BFF`).

**Separate system — Hemsidan published site** (`site.css`): Inter + noticeboard neutrals; accent themes (`amber` default `#9A5B1F`, `skog`, `hav`, `sten`). Not the product chrome.

Hardcoded one-offs that bypass tokens appear in places (e.g. scrollbar hover `#B3ACA6`, PDF pane `#FFFFFF`, amber button hover `#6F4A04`) — mostly intentional companions to token roles.

## Typography

Vendored woff2 under `brfv2-mockup/src/assets/fonts/` (and mirrored on mobile). No Google Fonts fetch (`font-src 'self'`).

| Role | Family | Job |
|---|---|---|
| Display / assertion | Poppins 500/600, tracking −0.015em | Page titles, brand name, `.ui-section-title`, chat titles |
| UI / explanation | Inter 400/500/600 (+ italic) | Body, labels, chrome, `.citat`, answer body (17px) |
| Measurement | JetBrains Mono 400–700 | States, dates, page counts, eyebrows, `.matt`, `.kbd` |

Desktop scale (`theme.css`): `--text-xs` 12 → `--text-sm` 14 → `--text-base`/`--text-md` 15 → `--text-lg` 17 → `--text-xl` 24 → `--text-2xl` 30. Body line-height **1.6**. The serif assertion floor is gone — assertion is carried by Poppins 600 + tight tracking at the same sizes. `--font-serif` survives only as a deprecated alias of `--font-sans`; Instrument Serif woff2 is deleted from `assets/fonts/`.

**Sub-12px contrast rule (beslut, 2026-08-08):** mono/meta text under 12px uses `--ink-muted` (`#605A57`), never `--ink-subtle`, to hold WCAG AA on the paper ground.

Mobile PWA body is **16px** (`--t-body: 1rem`); the PWA answer layer is still Instrument Serif at 22px — deliberate divergence until the mobile pass.

## Spacing, Sizing & Layout

- Named spacing scale on desktop since 2026-08-08: **`--s1…--s14`** (4px base, up to 56px). Mobile PWA has explicit `--s1…--s10`.
- Shell: sidebar **260px** with grouped nav (`Fråga & arkiv` / `Arbete` / `Post & ekonomi` mono section labels), main content padding **28×32×40**, chat column max **780px**.
- Controls: **`--h-control` 36px** default height (sm **30px**), **`--r-control` 10px** radius, **`--r-card` 14px** card radius; badges/counts **20px**.
- Page pattern: `.page-header` + Poppins `.page-title` (`--text-2xl`) + muted subcopy (`max-width` ~72ch).
- Breakpoints: **locked to 1200 / 1024 / 768 / 560** (beslut 2) — `design-lock.test.js` fails on any other `@media` width under `src/`.
- Density: operational lists/tables; empty states are one shared pattern (`.ui-empty`, see Components).

## Surfaces: Radius, Borders, Shadows

| Token | Approx | Use |
|---|---|---|
| `--r-control` | 10px | Controls (buttons, inputs) — canonical since 2026-08-08 |
| `--r-card` | 14px | Cards / panels, incl. the invoice ledger and empty-state card |
| `--radius-sm` … `--radius-xl` | 6–14px | Small chrome (legacy scale, still in use) |
| `--radius-2xl` / `--radius-3xl` | 18px / 24px | Legacy control/card radii — new work uses `--r-control` / `--r-card` |
| `--radius-block` | 12px | Multi-line content (textarea, answer sheet, tall prompts) |
| `--radius-pill` | 999px | Badges, segmented tabs |

Borders: 1px warm greys. Shadows: soft ink-tinted `--shadow-xs` → `--shadow-lg` (popover / modal). Elevation model: paper ground → white card/sidebar → modal over `rgb(55 50 47 / 0.38)` scrim. Dividers via 1px `--border` more than heavy cards everywhere; cards used for interactive/work surfaces (`.ui-card`, `.glass-panel`, chat empty, tables).

## Components & Patterns

Canonical primitives live in **`theme.css`** (`.ui-btn`, `.ui-input`, `.ui-badge`, `.ui-tabs`, `.ui-card`, `.ui-table`, `.ui-empty`, `.ui-notice`, `.ui-section-title`, `.ui-link`, `.matt`, `.citat`, `.kbd`, `.traff-mark`).

| Pattern | Defining properties | Where |
|---|---|---|
| Primary button | Ink fill, white text, 36px `--h-control`, 10px `--r-control`, 15px icon | `.ui-btn--primary` — the only button primitive (beslut 1, design-locked) |
| Outline / secondary / ghost | Border or muted fill | `.ui-btn--*` |
| Destructive button | Soft red wash + border (Avbrott) | `.ui-btn--destructive` |
| Amber filled warning button | Solid `--warn` + white | `.ui-btn--warning` (chat report) — deliberate exception, kept visible |
| Links | Handling blue, underline at 40% opacity | `.ui-link` |
| Nav | Grouped sections with mono labels; 34px items, muted → selected muted surface | `NAV_GROUPS` + `.sidebar-section-label` / `.nav-item` |
| Brand lockup | Mark + Poppins name + mono qualifier | `.sidebar-brand` / login |
| Answer / refusal | Inter 17px body; mono uppercase state; TraffMark | `.chat-content--answer`, `.answer-state--*` |
| Citations | Belagt green number; openable pills | `.citation-*` |
| PDF highlight | Remsgul wash + edge | `App.css` PDF / `mark.search-highlight` |
| Empty state | Dashed `--r-card` card, 48px icon tile, one strong-led sentence, ≤1 action; `ok` tone for “all handled” | `EmptyState.jsx` + `.ui-empty` — one pattern in every section |
| Keyboard hint | Mono `/` chip in resting filter fields, hidden on `:focus-within` | `.kbd` |
| Modals | Centered card, soft scrim | `.modal-backdrop` / `.confirm-dialog` |
| Domain workspaces | Per-file CSS composing same tokens | Intake, Invoices, Tasks, Watches, Integrations, Website shell |

**Icons:** `lucide-react`, typically 13–18px (15px in buttons), muted unless state-coloured.

## Iconography & Imagery

- Product mark: concentric ring + core (`TraffMark`) — brand (always filled, monochrome) vs status (`vila` / `soker` / `belagt` / `ejbelagt`). Below 16px → solid dot. Geometry: ring 8% of diameter, core 46% of inner measure.
- No illustration/photo system in product chrome. PDF page imagery is evidence (never inverted).
- Published site may use member-facing imagery via blocks; separate from product.

## Motion & Interaction

- Short transitions: **0.12–0.15s** ease on hover colours; sidebar **0.24s**; skeletons pulse **1.8s**; spinner **1s** linear.
- Focus: **one ring** — 2px `--ring` (Handling blue) with 2px offset on every control. List rows draw it inset (`box-shadow: inset 0 0 0 2px`) plus a `--handling-soft` tint so card rounding never clips it. The 3px glow token is deleted; design-lock fails on its return. Sidebar nav keeps a dense 2px box-shadow ring (no offset) by deliberation.
- Keyboard flow (2026-08-08): `/` focuses the view's own filter (`useSlashFocus`), ↑/↓ move the mark in the Inkommande thread list and Fakturor register, Enter opens the marked item, Esc steps back / empties the filter.
- Hover: surface shifts (`--surface-2` / `--muted-hover`), not hue flips on primary (primary darkens ink).
- `prefers-reduced-motion: reduce` collapses animation/transition durations.
- Mobile PWA names motion tokens (`--ease`, `--d-push` 180ms, `--d-sheet` 240ms).

## Visual Rhythm & Composition

- **Shell:** optional scope strip → white sidebar (brand, grouped nav under mono section labels, BRF + user footer) → paper main canvas.
- **Workspace opener:** one Poppins masthead + short muted intro; work below in bordered white surfaces or split panes (intake/invoices denser).
- **Ask loop:** question in sans with mono “FRÅGA”-style label; answer as bordered sheet in Inter 17px; citations and PDF reader as provenance, not chat bubbles.
- Hierarchy: type role first, then weight/spacing, then meaning colour. Colour is scarce on purpose.
- Alignment: left-led reading columns; tables as “registers” with mono column headers and tabular numeric columns (invoice ledger carries the register language: 34px mono headers, `--r-card` wrap).

## Distinctive Choices

1. **Colour locked to epistemic meaning** — green = verified passage; amber = no proof; blue ring = searching; red = machine break only; primary CTA is ink.
2. **Three type cuts, three jobs** — display sans asserts, sans explains, mono measures; never mixed for the wrong job. (Until 2026-08-08 the assertion cut was Instrument Serif; it is now Poppins by deliberate decision.)
3. **Paper desktop vs dark-shell phone** — same identity, inverted ground for long reading sessions vs phone “mörkt skal, lysande papper”.
4. **Träff mark as status language** — empty until belagt; brand mark never wears state colour.
5. **Document is the only pure white** on desktop (PDF); workspace is warm paper.
6. **Overdue is mono weight/edge, not red** (`.matt--overdue`, `.rule-overdue`).
7. **Hemsidan canvas is a second design system** so editors see what members see.
8. **Durability by test, not convention** — one button primitive, four breakpoints and one focus ring are enforced by `design-lock.test.js`, not by reviewer memory.

## Inconsistencies & Competing Directions

Resolved 2026-08-08 (unified-system pass): dual button CSS (`.primary-action-btn*` deleted, design-locked), breakpoint sprawl (four locked widths), the 3px focus glow (one ring, token deleted), serif/sans split on desktop (sans-first), ad-hoc empty states (one `EmptyState` pattern).

Remaining:

- **Type divergence across clients:** desktop display/UI = Poppins/Inter; mobile PWA still asserts in Instrument Serif 22px; native untouched. Converge or keep as a form-factor exception.
- **Primary action colour differs by client:** desktop primary = ink; mobile PWA light `--action` and native `--action` are Handling/electric blue for filled buttons.
- **Token naming drift:** desktop `--belagt`/`--vagran`/`--papper` vs mobile `--grounded`/`--refusal`/`--paper` vs native TS object — same identity, three vocabularies.
- **Native Avbrott** `#E5484D` vs desktop/PWA `#A82F22` (different reds for “error”).
- **Native/PWA dark handling blues** `#2E6BFF` / `#7EA2FF` vs desktop paper Handling `#1A4FD0`.
- **`site.css` header comment** still describes a “dark application shell”; product chrome is paper — comment lag, not necessarily live dark UI.
- **Amber filled `.ui-btn--warning`** uses Vägran as a solid CTA fill (report flow) — a deliberate, visible exception to “amber = no proof”.
- **Legacy radius scale** (`--radius-2xl`/`--radius-3xl`) coexists with `--r-control`/`--r-card`; migrate surfaces opportunistically, new work uses the new tokens.
- Identity source HTML not in repo — values are transcribed into CSS with long comments; cannot cross-check against original here. And since 2026-08-08 the desktop no longer follows its serif layer at all.

## Open Questions

- Should the phone clients adopt the sans-first assertion layer (Poppins/Inter), or is the serif answer a deliberate phone-era exception?
- Whether desktop will ever ship a dark shell (token comments argue against adding `--skal` casually).
- Whether mobile blue primary CTAs should converge on desktop ink-primary or remain form-factor exceptions.
- Locate or archive `Traff-identitet-v2.dc.html` in-repo for future agents — noting the desktop has now deliberately departed from its typography while keeping its colour semantics.
- Evidence for the unified system lives in `docs/design/2026-08-08-unified-system/shots/` and `docs/evidence/inkommande-*.png` — refresh when the next section pass lands.
